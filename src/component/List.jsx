//coponent that takes in the prop that is the list of players or teams and then maps through them and returns a link of each one
//name detail id, pass them in

import React from 'react'

export default function List() {
    return (
        <div>
            
        </div>
    )
}
